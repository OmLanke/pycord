interface:
  display_name: "Pycord"
  short_description: "Repo-specific patterns and workflows for pycord"
  default_prompt: "Use the pycord repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true